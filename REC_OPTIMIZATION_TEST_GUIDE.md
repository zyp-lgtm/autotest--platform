# 录制优化功能测试指南

## 测试环境
- **前端**: http://localhost:3000
- **后端**: http://localhost:8000
- **测试账号**: demo / demo123

## 测试目标

验证 Phase 1-3 的录制优化功能：
- ✅ Phase 1: 输入去重、智能等待、录制配置
- ✅ Phase 2: 智能变量提取（60+ 字段类型）
- ✅ Phase 3: 变量编辑、步骤预览、变量替换

---

## 测试场景

### 场景 1: 登录功能录制

**目标**: 验证输入去重和变量提取

#### 步骤:

1. **打开录制向导**
   - 登录系统
   - 进入"场景管理"页面
   - 点击"新建场景" → 选择"🎬 录制创建"

2. **步骤1: 录制准备**
   - 输入场景名称: `登录功能测试`
   - 配置选项（保持默认）:
     - ✅ 启用智能等待
     - ✅ 自动提取变量
     - ✅ 合并连续输入
   - 点击"开始录制"

3. **步骤2: 执行录制**
   - 在打开的浏览器中:
     - 导航到: http://localhost:3000 (或任何测试页面)
     - 输入用户名: `testuser` (**测试点**: 输入过程中不要停顿)
     - 输入密码: `password123`
     - 点击登录按钮
   - 回到录制向导，点击"停止录制"

4. **步骤3: 验证智能数据提取** ✨
   - 检查是否识别出以下变量:
     - `username` 或 `email` (置信度应该 > 60%)
     - `password` (置信度应该 > 60%)
   - **测试点**:
     - 修改变量名（如将 `username` 改为 `account`）
     - 添加新的测试值（如添加 `admin`, `user2`）
     - 查看置信度可视化（进度条颜色）

5. **步骤4: 验证结果预览** ✨
   - 检查生成的步骤:
     - 步骤1: 导航操作（应该在最前面）
     - 步骤2: 可能包含等待元素步骤（如果启用了智能等待）
     - 步骤3-5: 输入和点击操作
   - **测试点**:
     - 展开步骤参数查看详情
     - 检查变量汇总区域（显示提取的变量）
     - 查看统计信息（步骤数、配置状态）

6. **保存场景**
   - 点击"确认保存"
   - 场景应该出现在场景列表中

---

### 场景 2: 电商搜索录制

**目标**: 验证电商场景的字段识别和变量提取

#### 步骤:

1. **打开录制向导**
   - 场景名称: `商品搜索测试`

2. **执行录制**
   - 导航到任何电商网站或测试页面
   - 在搜索框输入: `iPhone 15`
   - 选择分类: `电子产品`
   - 输入价格范围: `5000-8000`
   - 点击搜索按钮

3. **验证变量提取** ✨
   - 检查是否识别出:
     - `search_keyword` 或 `keyword`
     - `category` 或 `product_category`
     - `price` 或 `price_range`
   - **测试点**:
     - 置信度评分（搜索词应该置信度很高）
     - 字段类型标注（input, select 等）

4. **添加自定义变量**
   - 点击"+ 添加自定义变量"
   - 输入变量名: `brand`
   - 添加测试值: `Apple`, `Samsung`, `Xiaomi`

5. **保存并查看结果**

---

### 场景 3: 配置选项测试

**目标**: 验证不同的配置选项效果

#### 测试案例:

##### 案例 3.1: 关闭智能等待
1. 场景名称: `测试 - 无智能等待`
2. 配置:
   - ❌ 启用智能等待
   - ✅ 自动提取变量
   - ✅ 合并连续输入
3. 执行简单录制（输入用户名 + 点击按钮）
4. **验证**: 生成的步骤中**没有** WAIT_FOR_ELEMENT 步骤

##### 案例 3.2: 关闭输入合并
1. 场景名称: `测试 - 无输入合并`
2. 配置:
   - ✅ 启用智能等待
   - ✅ 自动提取变量
   - ❌ 合并连续输入
3. 执行录制（输入时故意停顿）
4. **验证**: 应该捕获多个输入事件（实际效果需要查看录制数据）

---

## 验证清单

### Phase 1 功能验证 ✅

- [ ] **输入去重**: 快速输入用户名，只记录最终值
- [ ] **智能等待**: 生成的步骤包含 WAIT_FOR_ELEMENT
- [ ] **配置选项**: 三个配置选项正常工作
- [ ] **导航优先**: navigate 操作总是第一个步骤

### Phase 2 功能验证 ✅

- [ ] **字段识别**: 正确识别 username, password, email, phone 等
- [ ] **电商字段**: 正确识别商品名、价格、数量、订单号
- [ ] **置信度评分**: 合理的置信度值（60-100%）
- [ ] **字段分组**: 相关字段正确分组

### Phase 3 功能验证 ✅

- [ ] **变量名编辑**: 可以修改变量名
- [ ] **添加新值**: 可以手工添加测试值
- [ ] **置信度可视化**: 进度条正确显示颜色
- [ ] **自定义变量**: 可以添加自定义变量
- [ ] **步骤预览**: 可以展开查看参数
- [ ] **变量汇总**: 显示提取的变量列表
- [ ] **变量替换**: 参数中使用 `${变量名}` 格式

---

## 常见问题排查

### 问题 1: 录制浏览器未打开
**可能原因**: Playwright 未正确安装
**解决方案**:
```bash
cd backend
playwright install chromium
```

### 问题 2: 没有捕获到任何操作
**可能原因**:
- 没有在打开的浏览器中操作
- 录制脚本未加载

**检查方法**:
- 在浏览器控制台查看是否有 `[录制]` 日志
- 点击"🧪 测试捕获功能"按钮

### 问题 3: 变量提取失败
**可能原因**:
- 操作为空或不符合模式
- 字段名猜测未匹配到已知类型

**解决方案**:
- 手工添加自定义变量
- 修改字段名

### 问题 4: 智能等待未生效
**检查**:
- 确认"启用智能等待"配置已勾选
- 查看生成的步骤是否包含 `WAIT_FOR_ELEMENT`

---

## 预期结果

### 成功的录制应该包含:

1. **导航步骤**: 第一步总是 navigate 操作
2. **等待步骤**: 如果启用智能等待，操作前应该有等待
3. **变量引用**: 参数应该是 `${username}` 而不是 `testuser`
4. **原始值**: 保留 `_original_value` 供参考
5. **元数据**: 显示操作数、变量数等统计信息

### 示例生成的场景:

```json
{
  "name": "登录功能测试",
  "cases": [{
    "name": "主流程",
    "steps": [
      {
        "step_name": "导航到 http://localhost:3000",
        "keyword_id": "kw_NAVIGATE",
        "parameters": { "url": "http://localhost:3000" }
      },
      {
        "step_name": "等待元素就绪: #username",
        "keyword_id": "kw_WAIT_FOR_ELEMENT",
        "parameters": {
          "selector": "#username",
          "state": "visible",
          "timeout": 5000
        }
      },
      {
        "step_name": "在 #username 输入",
        "keyword_id": "kw_INPUT",
        "parameters": {
          "selector": "#username",
          "text": "${username}",
          "_original_value": "testuser"
        }
      }
      // ... 更多步骤
    ]
  }]
}
```

---

## 测试报告模板

完成测试后，请记录：

| 测试场景 | 状态 | 问题描述 | 严重程度 |
|---------|------|---------|---------|
| 场景1: 登录功能 | ⬜ 通过 / ❌ 失败 | | |
| 场景2: 电商搜索 | ⬜ 通过 / ❌ 失败 | | |
| 场景3.1: 无智能等待 | ⬜ 通过 / ❌ 失败 | | |
| 场景3.2: 无输入合并 | ⬜ 通过 / ❌ 失败 | | |

**总体评价**: ⬜ 优秀 / ⬜ 良好 / ⬜ 需要改进

**建议和反馈**:
-

---

**祝测试顺利！** 🎯
